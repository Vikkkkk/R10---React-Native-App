import { Platform, StyleSheet } from "react-native";
const styles = StyleSheet.create({
  listTitle: {
    color: "#9963ea",
    fontSize: 20,
    paddingBottom: 10
  },
  description: {
    lineHeight: 19,
    fontSize: 18,
    paddingBottom: 10,
    paddingTop: 5
  }
});

export default styles;
